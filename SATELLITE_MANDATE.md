## 🛰️ 위성 슬리브 원칙 (Satellite Mandate)

> 이 문서는 **−30% 구간의 나**를 위해 미리 적은 것이다.
> 그때는 반드시 "이번은 다르다"는 이유가 떠오른다. 그 이유는 여기에 없다.
> 손실을 본 뒤에 이 문서를 고치는 것은 규칙을 지킨 것이 아니라 없앤 것이다.
> 고칠 일이 생기면 §7 개정 이력에 **날짜·근거·무엇을 바꿨는지**를 남긴다.

*이 파일은 앱 위성 섹션에서 그대로 렌더된다. 문안을 app.py 에 복사하지 말 것 — 두 벌이 되면 반드시 갈라진다.*

---

### 1. 확정 숫자

| 항목 | 값 |
|---|---|
| 위성 슬리브 | HSA 총액의 **1/3** |
| └ A반 (`hsa-위성-12-0`) | 1/6 |
| └ B반 (`hsa-위성-blend`) | 1/6 |
| 각 반 보유 종목 | 최대 **5종목 · 등가중** |
| 리밸런싱 주기 | **주간** — 매주 월요일 집행 · 직전 금요일 종가 기준 |
| 두 장부 사이 리밸런싱 | **금지** |
| A/B 실험 개시일 | **2026-09-07** |
| 나머지 2/3 | 코어 ETF — 이 문서의 적용 대상이 아니다 |

**두 장부 사이를 리밸런싱하지 않는 이유:** 한쪽이 앞서면 옮기고 싶어진다. 옮기는 순간 측정하려던 격차가 사라진다. 그 격차가 이 실험의 결과값이다.

### 2. 랭킹 규칙

- **A반** — `mom12_0` (12개월 수익률, 0개월 skip). 소요 이력 253봉.
- **B반** — `1M×40% + 3M×40% + 6M×20%` (2026-09-05 이전 방식).
- 후보 풀 — GICS 섹터당 1개 ETF + 테마 ETF (약 60개).
- **SSOT: `fmp_extras.compute_satellite_top10`.** 앱과 주말 Hidden Alpha 이메일이 같은 함수를 쓴다. 손으로 순위를 다시 매기지 않는다.

**A반을 12-0 으로 바꾼 근거:** 워크포워드 6개 창 중 6개에서 blend 를 이겼다 (2026-09-05, `diag_momentum_rule_compare` T2). 그러나 후보 풀 편향(T6) 때문에 **백테스트만으로는 절대 우열을 말할 수 없다.** 그래서 blend 를 지우지 않고 B반으로 남겨 실제 돈으로 짝지어 비교한다.

**그 6/6 이 어느 창에서 나왔나 — 두 번째 한계 (2026-09-10 추가):** 평가 구간은 약 1,254봉(≈4년)이었다. 당시 저장소는 그것을 **FMP 의 이력 상한**으로 적어두고 있었으나, 실측 결과 단일 호출의 실제 상한은 **롤링 5,000 레코드(≈19.8년)** 였다(`diag_hist_ceiling`). 1,826일 제한은 우리가 스스로 건 정책 상수였고, 그 상수 때문에 저장소 어디에서도 더 깊은 요청이 나가지 않아 **자기 반증이 구조적으로 막혀 있었다.**

즉 **2022년 하락의 대부분이 평가 밖이었던 것은 데이터의 한계가 아니라 설정의 결과**다. T6(후보 풀 편향)와 원인이 다른 **독립적인 두 번째 한계**이며, "백테스트로 우열을 말할 수 없다"는 결론을 약화시키는 게 아니라 강화한다.

**이 사실로 A반 규칙을 바꾸지 않는다.** 더 깊은 창에서 12-0 이 더 좋아 보이든 나빠 보이든 마찬가지다 — 백테스트 비교는 §5 의 제외 항목이고, 실제 돈으로 짝지어 비교하는 것이 이 실험의 설계다.

### 3. 시장 필터 (기존 합의 · 유지)

**SPY 가 200일선 아래면 → 신규 매수 중단 · 비중 축소.**
앱 위성 블록 상단에 🚦 로 뜬다. 이건 예측이 아니라 손실 방지 장치다.

### 4. 종료·축소 트리거 — 셋은 서로 독립이다

같은 사건의 세 가지 얼굴이 아니라, **다른 원인**을 잡는 세 개의 장치다. 하나가 울렸다고 다른 둘이 면제되지 않는다.

#### ① 전제 붕괴 — 신호가 죽었나

- **주기** 6개월
- **무엇을** `diag_momentum_rule_compare` 실행 → T2 (12-0 vs blend) 워크포워드 결과
- **기준** **6개 창 중 4개 이상**. 미달이면 `Satellite_Mandate` 연속 카운트 +1
- **종료 검토** 3회 연속 미달 (= 18개월)
- **첫 점검** 2027-03-08 — `Reminders` 시트에 등록됨

기준은 **결과를 보기 전에 정해졌다.** 미달했을 때 "5/6 은 아니지만 방향은 맞았다" 식으로 다시 협상하지 않는다.

**⚠️ 2027-03-08 재실행의 비교 가능성 — 사전 결정 (2026-09-10, 결과 보기 전).**

조회 상한이 정책 상수임이 밝혀져 이제 훨씬 깊은 창을 쓸 수 있다. 그런데 창 깊이를 바꾸면 워크포워드 **6개 창의 구성 자체가 달라진다.** 그러면 "4/6 미달"이 신호가 죽었다는 뜻인지 자를 바꿨다는 뜻인지 구분되지 않는다.

**실측 기준값 (2026-09-10 기록).** `diag_momentum_rule_compare` 실행 로그:

> `[STEP 1] 수신 창 — SPY 1255봉 2021-09-10 ~ 2026-09-10 (전 종목 봉수 1255~1255)`

- **핀은 봉수가 아니라 창 길이 — 1,826 달력일**이다. 봉수는 휴장일 배치 때문에 기준일마다 1~2봉 흔들린다. 2027 재실행의 판정 기준은 "1255봉"이 아니라 "1,826달력일 창"으로 읽는다.
- 지금 1255봉이 나오는 이유는 `HISTORY_BARS=1300` 요청이 `HIST_MAX_DAYS=1826` 클램프에 깎이기 때문이다. **상한을 올리면 같은 코드가 조용히 1300봉을 받는다** — 핀이 저절로 풀린다.
- 따라서 상한 상향 작업에는 **창 길이를 명시적으로 고정하는 수단**이 함께 들어가야 한다. 봉수→달력일 환산에 기대면 상한이 바뀔 때마다 핀이 흔들린다.
- 부수 확인: 전 종목 봉수가 1255~1255 로 균일하다 → 현재 후보 풀에는 §2 의 `127봉 미만 제외` 에 걸리는 종목이 없다.

- **판정용 재실행은 2026-09-05 와 같은 창 깊이(1,826달력일)로 돌린다.** 트리거는 신호의 생존을 재는 장치이지 측정 도구를 갱신하는 자리가 아니다. 도구와 대상을 같이 바꾸면 결과는 해석 불가가 된다.
- 깊은 창 결과는 **참고용으로 따로** 돌린다. 4/6 판정에 쓰지 않는다.
- 깊은 창을 판정 기준으로 승격하려면 **새 사전 약정**이 필요하다. 그 약정은 결과를 보기 전에 쓴다.

이 결정을 지금 적는 이유는 §4① 첫 줄과 같다 — 2027년 3월에 숫자를 본 뒤에 정하면, 그때는 반드시 마음에 드는 쪽을 고르는 이유가 떠오른다.

#### ② 실행 실패 — 내가 안 따랐나

- 매주 리밸런싱 지시 대비 **실제 실행 여부**를 기록한다
- 성과가 나쁠 때 **전략 문제인지 실행 문제인지**를 먼저 가른다. 이걸 못 가르면 멀쩡한 전략을 버리거나 망가진 전략을 붙들게 된다

**층 1 — 지시 기록: 구축 완료.** `Satellite_Instruction` 시트에 `automation/run_hidden_alpha.py` 가 주말 실행마다 두 장부의 지시를 1행씩 남긴다. 열은 `Date | Book | Rule | Top_JSON | Bench_JSON | Risk_On | Pool_N | Skipped_N | Source`.

- `Date` 는 실행일(일요일)이 아니라 **지시 대상일 — 직전 금요일 종가**다. 실행 시각을 쓰면 같은 지시가 실행할 때마다 다른 날짜로 남는다
- 기록되는 순위는 **주말 메일에 뜬 바로 그 객체**다. 따로 다시 계산하지 않는다 — 다시 부르면 데이터 시점 차로 5위/6위가 뒤집혀, 층 2 가 재는 것이 "지시 대비 실행"이 아니라 "지시A 대비 지시B"가 된다
- `Risk_On` 은 §3 시장 필터의 그 주 상태다. `False` 인 주의 지시는 "신규 매수 중단"으로 읽는다. 공란은 "안 쟀다"이고 `False` 는 "재서 위험 구간"이다 — 둘은 다르다
- `Bench_JSON`(6~10위)을 같이 남기는 이유: 5위와 6위의 점수차가 벌어져 있으면 이탈의 비용이 크고 붙어 있으면 작다. 이탈의 **성격**은 그 간격 없이 판정할 수 없고, 지나간 뒤에는 복원할 방법이 없다

- **⚠️ 층 2 — 실행 대비 이탈 판정: 미구축.** 실제 체결을 읽는 지점이 없어 이탈을 수치로 말할 수 없다. 2026-11-03 검토 이후 착수한다. 그때까지 이 조항은 **기록만 하고 판정하지 않는다**

#### ③ 자본 손상 — 얼마나 잃었나

- **대상** 슬리브 **전체 합산** (A반 + B반)
- **트리거** 고점 대비 **−30%**
- **조치** 1/3 → **1/6** 로 축소. **양쪽 장부를 동시에 절반으로** 줄인다
- **고점의 정의** 개시일 시드(2026-09-07) **및 그 이후 월말 스냅샷** 평가액 중 최고치. 장중 고점이 아니다
- **측정** `Satellite_Snapshot` 시트. `automation/run_satellite_snapshot.py` 가 매월 **마지막 거래일** 5PM 에 두 장부를 1행씩 기록한다. 판정은 `fmp_extras.satellite_drawdown` **하나**가 한다 — 앱과 주간 메일이 같은 함수를 쓴다

**개시일 시드를 넣은 이유:** 월말 스냅샷만 쓰면 첫 고점이 2026-09-30 이라, 그 전에 −30% 가 나도 비교할 고점이 없다. 초기 구간이 비면 낙폭이 낙관 쪽으로 왜곡된다.

**⚠️ 현금은 추적하지 않는다 — 이 수치의 알려진 한계**

`Cash` 열은 공란이고, 평가액은 보유 시가뿐이다. 편향은 두 방향으로 난다.

| 상황 | 측정 낙폭 | 발동 |
|---|---|---|
| 고점은 만기 투자였고 이후 §3 필터로 현금화 | **과대** | 실제보다 **일찍** |
| 고점 시점에 이미 현금을 들고 있었다 | 과소 | **늦게** |

두 번째가 남는 위험이다. 다만 §3 필터가 켜진 국면은 하락장이라, 그 달 평가액이 고점이 될 확률은 낮다.

**이 한계를 이유로 트리거를 완화하지 않는다.** 손실을 보기 전에 예외 구멍을 파 두는 것은 §5 가 막으려는 바로 그 행동이다. 일찍 발동하는 쪽은 비용이 작고 늦게 발동하는 쪽은 비싸다 — 그래서 −30% 는 그대로 둔다.

**보유 0종목인 장부가 있는 달은 판정에서 빠진다.** 그 장부의 평가액은 0 이 아니라 **모르는 것**이기 때문이다. 0 으로 합치면 전량 현금화한 달에 슬리브가 반토막 난 것으로 찍혀 −50% 가 가짜로 발동한다 — 규칙을 지킨 행동이 트리거를 당기게 된다. 그런 달은 로그에 `평가 불가` 로 남는다.

현금을 관리하기 시작하면 `Cash` 열을 채우는 것만으로 위 한계가 전부 사라진다.

**장부별이 아니라 전체 합산인 이유:** A반만 줄이면 두 장부의 자본이 달라지고, 그 시점 이후의 A/B 비교는 전부 오염된다. 실험을 지키려면 두 장부는 항상 같이 움직여야 한다.

### 5. 판정에서 **제외**하는 것 — 사전 차단

아래 항목으로는 슬리브를 종료·축소·변경하지 않는다.

- 개별 종목의 손익 (한 종목이 −40% 여도 그것만으로는 아무 의미가 없다)
- **6개월 미만**의 성과
- QQQ / VOO / MTUM 대비 상대 성과
- 백테스트 기간 수익률과의 비교 (후보 풀 편향 T6 — 애초에 비교 가능한 수치가 아니다)

**이유:** 이 슬리브의 추적오차는 연 약 **17.5%** 다. 이 정도 변동성에서 성과 기반 종료 기준은 통계적으로 신뢰할 수 없다 — 운과 실력을 가를 표본이 몇 년 단위로 필요하다. (Cornell·Hsu·Nanigian, *Journal of Portfolio Management*, 2017: 성과 기반 매니저 교체는 체계적으로 역효과)

그래서 ①②③ 은 **전부 성과 기준이 아니다.** ①은 신호의 생존, ②는 나의 실행, ③은 자본의 절대 손상이다.

### 6. 지금 실제로 작동하는 것

정직하게 적는다. 문서에 썼다고 작동하는 게 아니다.

| | 상태 |
|---|---|
| ① 전제 붕괴 | ✅ `Reminders` 등록 완료 — 만기 7일 전부터 매일 메일에 뜬다 |
| ② 실행 실패 | ⚠️ 부분 구축 — 층 1(지시 기록) 작동. 층 2(실행 대비 이탈 판정) 미구축, 2026-11-03 이후 |
| ③ 자본 손상 | ⚠️ 부분 구축 — 월별 스냅샷 작동, 앱·주간 메일에 낙폭 표시. 단 **현금 미추적**(§4③ 한계). 현금을 관리하면 ✅ 로 올린다 |
| 시장 필터 | ✅ 앱 위성 블록 상단 🚦 |

②③ 이 켜지기 전까지 이 문서의 해당 조항은 **의도 선언**일 뿐이다.

### 7. 개정 이력

| 날짜 | 내용 | 근거 |
|---|---|---|
| 2026-09-07 | 최초 작성. §1 숫자 확정, §4 트리거 3종, §5 제외 항목 사전 차단 | 2026-09-05 `diag_momentum_rule_compare` T2 6/6 → A반 12-0 채택 · 2026-09-07 A/B 분할 개시 |
| 2026-09-07 | §4③ 구축. 고점 정의에 **개시일 시드** 추가, 측정 경로·현금 미추적 한계 명시. §6 ③ ❌ → ⚠️ | 월말 스냅샷만으로는 첫 달 낙폭을 잴 수 없다(초기 구간 공백). **트리거 −30% 는 변경하지 않았다** — §5 |
| 2026-09-08 | **§1 오기 정정** — 리밸런싱 주기 **월간 → 주간**. §4③ 월말 스냅샷 주기가 §1 리밸런싱 주기로 번진 오기였다. **§4③ 스냅샷은 월간 그대로**다 | 개정이 아니라 오기 수정이다. 근거를 새로 만들지 않았다 — 채택 근거인 2026-09-05 워크포워드가 이미 주간 축(`diag_momentum_rule_compare` `FIXED_FREQ = "weekly"`)에서 돌았고, 실제 집행도 주간(월요일)이었다. 개시 이틀째·손실 0·성과와 무관. **트리거 숫자는 하나도 바꾸지 않았다** — §5 |
| 2026-09-10 | **§2 각주 추가 · §4① 사전 결정 추가.** 조회 상한이 API 한계가 아니라 정책 상수임을 기록하고, T2 를 포함한 2026-09-05 판정들의 평가 창(≈1,254봉·4년)을 명시. 2027-03-08 재실행은 **같은 창 깊이**로 돌린다고 사전 결정 | 실측(`diag_hist_ceiling`, 2026-09-10): 단일 호출 상한은 롤링 **5,000 레코드(≈19.8년)**. 4종목(AAPL/MSFT/SPY/QQQ)이 전부 5,000봉·같은 최소일을 냈고, 두 실행 사이 하루가 지나자 바닥도 하루 밀렸다. `to` 를 과거로 미니 창이 따라 이동했다. 19개 완전 연도의 봉수가 250~253 — 중간 절삭 없음. `HIST_MAX_DAYS=1826` 은 자기 반증을 막고 있었다(그 이상 요청이 나가지 않으므로). **트리거 숫자·랭킹 규칙은 하나도 바꾸지 않았다** — §5 |
| 2026-09-10 | **§4① 실측 기준값 기록** — `1,826달력일 창 / SPY 1255봉 / 2021-09-10~2026-09-10`. 핀을 **봉수가 아니라 창 길이**로 규정 | 봉수는 휴장일 배치로 기준일마다 흔들린다. 또 현재 1255봉은 `HISTORY_BARS=1300` 이 `HIST_MAX_DAYS=1826` 에 깎인 결과이므로, **상한을 올리면 핀이 저절로 풀린다.** 상한 상향 작업에 창 길이 고정 수단을 함께 넣는 것을 조건으로 건다 |
| 2026-09-10 | **`AS_OF` 창 고정 도입** (`diag_satellite_backtest` v3.0, 락스텝 `diag_fmp_ssot` B4f~B4i). `[STEP 1]` 이 요청 창과 **수신 창**을 따로 기록 | `hist_range_params` 는 `from = 오늘 − N일` 이라 **같은 코드가 매일 다른 데이터를 본다.** 지금까지 그게 안 드러난 건 상한 클램프가 창을 늘 포화시켰기 때문이고, 상한을 올리면 그 완충이 사라진다. ⚠️ **재현을 보장하지 않는다** — `adjClose` 는 배당 지급 때마다 과거가 소급 재산정되므로 창을 고정해도 값은 달라진다. 고정되는 것은 "어느 구간을 봤는가"뿐이고, 그것만으로 "창이 바뀐 것"과 "룰이 다른 것"을 가른다. 따라서 **2026-09-05 판정들은 소급 동결이 불가능하다 — 수신 창 미기록 · 비트 재현 불가**로 남긴다 |
| 2026-09-08 | §4② **층 1(지시 기록) 구축.** `Satellite_Instruction` 신설, `run_hidden_alpha.py` [STEP 5.7]. §6 ② ❌ → ⚠️ | 층 2 판정은 기록이 쌓인 뒤에만 의미가 있다. 지금 기록을 시작하지 않으면 11월에도 잴 것이 없다. 판정 로직은 **일부러 만들지 않았다** — 근거 없는 판정은 없는 것보다 나쁘다 |
