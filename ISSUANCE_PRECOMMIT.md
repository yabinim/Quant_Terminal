## 📌 순주식발행 신호 — 사전 약정 (Pre-commitment)

- **작성일** 2026-09-12 · C 트랙 순주식발행 Phase 1
- **선행** `diag_issuance_probe` Phase 0 실행 2026-09-12 (163콜 · 실패 0) → 경로 **S** 확정
- **상태** ⛔ **판정 실행 없이 종료.** 근거는 §9 — 이 유니버스·이 구간으로는 문헌이 보고한
  크기의 효과를 통계적으로 구분할 수 없다. 설계가 틀려서가 아니라 **측정 장치의 분해능**
  문제다
- **이 문서를 남기는 이유** 종료 근거를 추정이 아니라 계산으로 고정하기 위해서다. 나중에
  이 트랙을 다시 열려는 사람(=미래의 나)이 §6 문턱과 §9 계산을 보지 않고 "이번엔 될 것
  같다"로 재시작하는 것을 막는다

---

### 1. 질문

순주식발행(자사주 매입 ↔ 희석)이 **개별 종목의 이후 수익률을 구분하는가.**

문헌: Daniel·Titman(2006), Pontiff·Woodgate(2008) — 발행 많은 기업이 이후 저조.
원논문 규모 연 6~8%p. 출판 후 감쇠(McLean·Pontiff 2016, 평균 −58%) 적용 시
**현실적 잔존 효과는 연 2.5~3.4%p**로 본다. 이 숫자가 §9 판단의 기준선이다.

적용 지점은 **개별 종목 워치리스트 플래그(1-A)** 하나다. 위성 룰 후보가 아니다 —
`SATELLITE_MANDATE` §2 의 후보 풀은 섹터·테마 ETF 약 60개이고, ETF 의 주식 수 변동은
설정/환매 flow 이지 희석이 아니다.

**PEAD 와 비대칭인 점:** 라이브에 검증할 기능이 아직 없다. 미달의 대가는 "만들지
않는다"이고 통과의 대가는 "새로 만든다"이다. 즉 이번엔 **통과 쪽이 비용**이라,
문턱을 느슨하게 할 이유가 없다.

---

### 2. 검증 대상 — 동결

| 항목 | 값 |
|---|---|
| 데이터 경로 | **S** — `income-statement?period=quarter` 의 `weightedAverageShsOut` + `filingDate` |
| 래그 상수 | **0** — `filingDate ≤ 형성일` 조건이 룩어헤드를 구조적으로 막는다 |
| 주식 수 정의 | **basic** 가중평균 (diluted 아님 — 옵션 회계 가정이 섞인다) |
| 분할 처리 | 불필요 — 이 필드는 분할 조정 완료 (Phase 0 I-ARTIF 0.74%) |

경로 M(시총 ÷ 종가)은 Phase 0 에서 I-LAG 57.2% · I-NOISE 0.1218% 로 탈락했다.
미달 원인이 `STEP_MIN`=0.3% 문턱이 실제 분기 변동보다 컸기 때문일 가능성이 높지만,
**재측정하지 않는다** — 결과를 보고 문턱을 고치는 습관이 이 문서가 막으려는 것이다.

---

### 3. 유니버스

**후보 풀** — `company-screener` 1콜.
`isEtf=false&isFund=false&isActivelyTrading=true&exchange=NASDAQ,NYSE&marketCapMoreThan=…`
라이브 Tier 2 빌더(`ec._screener_map`)와 **같은 함수**를 탄다. 유니버스 정의를 두 벌로
만들지 않기 위해서다.

**형성 시점별 실제 편입** — 스크리너는 오늘 스냅샷이라 그대로 쓰면 시총 기준에
룩어헤드가 섞인다. 그래서 편입 판정은 이미 받는 데이터로만 한다:

- 시점 기준 시총 = `weightedAverageShsOut`(filingDate ≤ 형성일) × 형성일 종가 ≥ **$500M**
- 형성일 종가 ≥ **$5**
- 두 분기(q, q−4) 재무제표가 모두 `filingDate ≤ 형성일`

추가 콜 0. 콜 예산은 스크리너 1 + 종목당 2(일봉 1 · 분기 재무제표 1) = 후보 1,500종목
기준 **약 3,000콜**.

---

### 4. 신호 구성

```
NSI = ln( shares_q / shares_{q−4} )
```

- `shares` = basic `weightedAverageShsOut`, 두 분기 모두 `filingDate ≤ 형성일`
- 낮을수록(자사주 매입) 롱, 높을수록(희석) 숏
- **M&A 꼬리 처리** — Phase 0 에서 +44.75% · +48.25% · +76.04% 관측. 자사주/희석이
  아니라 인수 대금 주식 발행으로 보인다. 세 겹으로 처리한다:
  1. **순위 기반 퀸타일** 정렬 — 크기에 둔감. 극단값은 그냥 5분위에 들어간다
  2. 사전 배제: `|NSI| > ln(2)` (12개월 주식수 ±100% 초과) 종목-분기 제외. 배제 건수를 결과에 보고
  3. 부수 보고(**판정 아님**): 상위 1% 꼬리 포함본/제외본을 나란히 — 꼬리가 결과를 만드는지 보이게

  M&A 여부를 직접 확인할 엔드포인트는 현 플랜에서 402 다. 그래서 이것은 "M&A 제거"가
  아니라 **"M&A 규모 발행 제거"**다. 이 한계를 그대로 적는다.

---

### 5. 측정

| 항목 | 값 |
|---|---|
| 형성 | **분기말** (3·6·9·12월 마지막 거래일) |
| 보유 | **3개월 · 비중첩** |
| 판정 구간 | **2012Q1 ~ 2021Q4 형성 40분기** (마지막 청산 2022-03-31) |
| 스프레드 | Q1(최저 NSI) 동일가중 − Q5(최고 NSI) 동일가중, 3개월 수익률 |
| 연율 환산 | 문헌 대조용 **보고만** — 판정에 쓰지 않는다 |

분기 비중첩을 고른 이유는 §9 다. 연 1회 형성이면 유효 관측이 10개뿐이고, 분기 형성
+ 12개월 중첩 보유는 자기상관 때문에 유효 관측이 13~15개로 주저앉는다. 비중첩 40개가
같은 데이터에서 얻을 수 있는 가장 많은 독립 관측이다.

판정 구간을 2007 까지 늘리지 않는 이유: 실험마다 구간을 새로 고르는 것 자체가 이
프로젝트가 막으려는 습관이고, 2008~09 은 이미 deep-window 작업에서 본 구간이다.
(§9 는 늘려도 결론이 같음을 보인다.)

---

### 6. 판정 기준 — 실행 전 고정

| | 조건 |
|---|---|
| **P1** 표본 | 분기마다 퀸타일당 ≥ 30종목 **이고** 유효 분기 ≥ 32/40 |
| **P2** 평균 | 평균 분기 스프레드 ≥ **+1.25%p** (= 연 5.0%p) |
| **P3** 중앙값 | 분기 스프레드 중앙값 > 0 |
| **P4** 연도 일관성 | 10년 중 연 스프레드 > 0 인 해 ≥ **7** |

- **P1~P4 전부 ✅ 라야 통과.** 하나라도 ❌ 면 미달이다
- **표본 부족(P1 ❌)도 미달이다.** "판정 보류"는 없다 — 입증 책임은 신호에 있다
- P2 의 +1.25%p 는 §9 에서 **거짓 통과 5%** 가 되도록 역산한 값이다. Phase 0 의
  I-DISP 분포를 보고 정한 값이 **아니다**(그것은 §8 금지 사항이다)

---

### 7. 결과에 따른 조치

| 판정 | 조치 |
|---|---|
| 미달 | 순발행 트랙 **종료**. 개별 종목 플래그를 만들지 않는다. C 트랙에서 닫는다 |
| 통과 | 1-A 워치리스트 배지를 **별도 약정**으로 신설. "검증됨" 같은 말을 붙이지 않고, 매수 신호 · 비중 · 진입 게이트에는 영향 0 |

§9 에 따라 **판정 실행은 하지 않았다.** 따라서 위 표는 집행되지 않았고, 아래 §9 의
종료가 실제 조치다.

**예측을 주장하지 않는 표시는 별개다.** "지난 12개월 주식 수 ±N%" 를 P/E 처럼 사실
그대로 보여주는 것은 이 약정의 대상이 아니다. 다만 그 표시에는 **어떤 수익률 주장도
붙이지 않으며**, 화면 문구에 "미검증 — 수익률 검증 없음"을 명시한다. PEAD 에서 배운
실패 모드는 신호 자체가 아니라 **예측을 주장하는 배지**였다.

---

### 8. 1회 전용 · 금지 사항

- **수익률을 한 번이라도 계산한 실행은 판정 실행이다.** 판정 실행은 1회다
- 결과를 본 뒤 금지: 문턱 · 형성 빈도 · 보유 기간 · 구간 · 유니버스 · 퀸타일 수 ·
  배제 규칙을 바꾸는 것, 특정 연도나 섹터를 빼고 다시 보는 것
- **§9 를 본 뒤 P2 문턱을 낮추는 것도 금지다.** 검정력이 낮다는 것은 문턱을 낮출
  근거가 아니라 **실행하지 않을 근거**다. 문턱을 낮추면 거짓 통과율이 §9 표대로 올라갈
  뿐이다(연 2.0%p 문턱에서 진짜 효과 0 인데도 17.2% 가 통과한다)
- 재개는 §9 재개 조건을 **먼저 §11 에 적은 뒤**에만 한다

---

### 9. 문턱 근거 — 합성 데이터 검정력 · **그리고 종료 판정**

실제 수익률은 한 줄도 계산하지 않고 합성 데이터로만 계산했다 (2026-09-12, 시뮬 40,000회).

**가정**
- 분기 스프레드 = 평균 + 연도공통충격(σ 1.5%p) + 잔차(t(5), σ 5.5%p)
  → 분기 σ ≈ 5.70%p · 연 σ ≈ 12.5%p (퀸타일당 약 200~300종목 기준)
- 생존편향 드래그 **0.75%p/년** — 후보 풀이 오늘 상장 종목이라, 대량 발행 후 상장폐지된
  기업이 빠진다. 방향은 **가설에 불리**(Q5 가 실제보다 좋아 보여 스프레드가 작아진다)

**§6 규칙 전체(P1~P4)의 통과 확률 — 유니버스 (나) 기준**

| P2 문턱 (분기 / 연) | 진짜 0%p | 진짜 3%p | 진짜 4%p | 진짜 6%p |
|---|---|---|---|---|
| +0.50 / 2.0 | **17.2%** | 42.7% | 52.1% | 71.2% |
| +1.00 / 4.0 | 9.9% | 30.3% | 39.1% | 59.3% |
| **+1.25 / 5.0 · P4 k=7** | **5.1%** | 18.7% | 25.8% | 44.0% |
| +1.50 / 6.0 | 4.1% | 16.6% | 23.7% | 41.0% |
| +2.00 / 8.0 | 1.3% | 7.2% | 11.3% | 24.0% |

거짓 통과를 PEAD 수준(≈1%)으로 누르면 진짜 3%p 효과의 검정력이 **7%** 로 떨어진다.
검정력을 쓸 만하게(≥50%) 올리면 거짓 통과가 **17%** 가 된다. 둘 다 성립하는 지점이 없다.

**탐지 가능 최소 효과(MDE) — 거짓통과 5% · 검정력 80%**

| 구성 | 분기 σ | n분기 | MDE (연%p) |
|---|---|---|---|
| (가) 160종목 · 2012~2021 | 7.35 | 40 | **12.3** |
| **(나) 1,000종목 · 2012~2021** | 5.70 | 40 | **9.7** |
| (나) + 산업중립 (σ 30%↓ 가정) | 4.10 | 40 | 7.2 |
| (나) · 2007~2021 로 확장 | 5.70 | 60 | 8.1 |
| (나) + 산업중립 · 2007~2021 | 4.10 | 60 | **6.0** |

**현실적 잔존 효과는 연 2.5~3.4%p 다(§1).** 가장 관대한 구성조차 MDE 6.0%p —
**원논문의 감쇠 전 크기**를 요구한다. 진짜 3%p 효과를 80% 검정력으로 잡으려면
**635분기(≈159년)** 가 필요하다.

**⛔ 결론: 탐지 불가. 판정 실행을 하지 않고 종료한다.**

유니버스 확장 (나) 가 (가)보다 나은 것은 맞다(MDE 12.3 → 9.7). 그러나 개선분은
개별종목 잡음에만 작용하고, 구속 조건은 **종목 수가 아니라 형성 시점 수**다.
퀸타일 롱숏 스프레드는 같은 분기 안에서 종목끼리 강하게 상관되어, 유효 표본이
1,000이 아니라 40이다. 10년치 데이터에 종목을 아무리 더해도 이 벽은 움직이지 않는다.

이것은 **이상현상이 존재하지 않는다는 판정이 아니다.** 이 장치로는 존재해도 보이지
않는다는 판정이다. 두 문장을 섞지 않는다.

**재개 조건** — 아래 중 하나가 성립하고, 그 사실을 §11 에 먼저 적은 뒤에만:
1. 상장폐지 종목을 포함한 시점 기준 유니버스를 얻는다 (생존편향 제거 + 분기 수 확대)
2. 1990년대까지 닿는 분기 주식 수 이력을 얻는다 (n분기 ≥ 120)
3. 분기 스프레드 σ 를 **1.4%p 이하**로 낮추는 구성을 실증한다 — 진짜 3%p 를 §6 문턱으로
   잡으려면 필요한 값이다. 현실적으로 가능해 보이지 않으며, 가능하다 해도 그렇게 헤지된
   포트폴리오의 수익 자체가 작아진다

단순한 시간 경과는 재개 조건이 **아니다.** 2036년까지 기다려 n=100 이 되어도 MDE 는
약 6%p 에 머문다.

**다른 후보에 대한 파급** — FIP · BHM 잔차 모멘텀도 연 단위 횡단면 이상현상이다.
같은 계산을 **만들기 전에** 먼저 돌려야 한다. 위성 룰 비교(어느 룰이 나은가)는 같은
데이터를 공유하는 짝비교라 분산이 훨씬 작으므로 이 벽에 걸리지 않는다 — 별개 문제다.

---

### 10. 알려진 한계

- **생존편향** — 후보 풀이 오늘 상장 종목이다. 대량 발행 후 상장폐지된 기업이 빠지므로
  Q5 가 실제보다 좋아 보인다. 방향은 가설에 **불리**하므로, 만약 통과했다면 신뢰할 만하고
  미달은 해석이 모호해진다. §9 에 0.75%p/년으로 반영했다
- **M&A 구분 불가** — `mergers-acquisitions` 계열 엔드포인트가 현 플랜에서 402 다.
  §4 는 M&A 를 제거하지 못하고 M&A **규모**만 제거한다
- **횡단면 분산 감소** — Phase 0 I-DISP: IQR 4.30%p(2014) → 2.47%p(2018) → 1.27%p(2021).
  정렬할 재료가 줄어드는 방향이다. §9 에 감쇠 가정으로 넣어 본 결과 통과율 차이는
  0.5%p 미만이라 결론에 영향이 없었다
- **가중평균 주식 수** — 실제 발행 잔량보다 매끄럽고 약 1.5개월 늦다. 방향은 보수적이라
  그대로 둔다
- **배당 미조정 종가** — 절대 수준에 배당 차이가 섞이나 스프레드에서 대부분 상쇄된다
- **거래비용 · 세금 무시** — 신호의 구분력만 묻기 때문이다. 실제 집행 가능성은 별개이며,
  분기 리밸런스 롱숏은 개인 계좌에서 집행 자체가 비현실적이다
- **σ 가정의 불확실성** — 5.5%p 는 문헌 기반 추정이다. 실제가 4.0%p 라면 MDE 는 9.7 →
  7.2%p 로 내려가지만, 여전히 잔존 효과 3.4%p 의 두 배가 넘는다. 결론은 뒤집히지 않는다

---

### 11. 개정 이력

| 날짜 | 내용 |
|---|---|
| 2026-09-12 | 최초 작성. Phase 0(`diag_issuance_probe`, 163콜, I-PATH=**S**) 결과 위에 설계 확정 — 유니버스 (나) 확장안, 분기 형성 · 3개월 비중첩, 순위 퀸타일, M&A 규모 배제. §9 합성 검정력(시뮬 40,000회 · 실제 수익률 0줄) 계산 결과 **MDE 9.7%p/년 ≫ 잔존 효과 2.5~3.4%p/년** → **판정 실행 없이 종료.** 재개는 §9 의 세 조건 중 하나가 성립할 때만 |
