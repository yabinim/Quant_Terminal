## 📌 PEAD 라벨 검증 — 사전 약정 (Pre-commitment)

> 이 문서는 **결과를 보기 전의 나**가 적은 것이다.
> 결과를 보면 "이 문턱은 너무 빡빡했다", "그해는 특수했다"는 이유가 반드시 떠오른다. 그 이유는 여기에 없다.
> 판정 실행 뒤 §1~§10 을 고치는 것은 약정을 지킨 것이 아니라 없앤 것이다.
> 고칠 일이 생기면 §11 개정 이력에 **날짜·근거·무엇을 바꿨는지**를 남긴다.

| | 상태 |
|---|---|
| 약정 | ✅ 2026-09-11 박제 — 이 파일의 첫 커밋 시각이 증거다 |
| 러너 | ✅ 2026-09-11 `automation/diag_pead_label_verify.py` (이 파일 커밋 **뒤에** 구현) |
| 판정 실행 | ✅ 2026-09-11 20:32 ET 완주(§11) — **두 라벨 모두 미달.** §7 문구 교체 대상 |

---

### 1. 질문

**라이브 `evaluate_pead` 가 붙이는 두 라벨이, 아무도 본 적 없는 과거 사건에서 라벨이 말하는 방향의 드리프트를 보이는가.**

| 코드 | 현재 문구 | 문구가 하는 주장 |
|---|---|---|
| `up_continue` | `🟢 상승 지속 후보` | 이후 같은 유니버스의 평균 사건보다 **더 오른다** |
| `down_break` | `🔴 하락 이탈 — 근거 훼손` | 이후 같은 유니버스의 평균 사건보다 **더 내린다** (보유 근거가 약해졌다) |

**묻지 않는 것:** 매매 전략의 수익성 · 다른 지평 · SUE 같은 다른 PEAD 정의 · 개별 종목 · 거래비용. 이 약정이 답하는 것은 **"라벨 문구의 예측 의미가 정당한가"** 하나다.

**사전 기대는 부정적이다.** 대형주 PEAD 는 2000년대 중반 이후 사실상 사라졌다는 연구가 있다(Martineau, *Critical Finance Review*, 2022). 미달이 나와도 놀랄 일이 아니며, 미달의 대가는 문구 두 줄이다(§7).

### 2. 검증 대상 — 동결

라벨은 **지금 라이브에 떠 있는 그대로** 검증한다. 변형은 1개다. 문턱·조건을 바꾼 라벨 변형은 계산하지 않는다.

- **판정 호출:** `ec.evaluate_pead(m, move)` — regime 을 넘기지 않는다. 라이브 `run_earnings_watch.py` 의 사후 패스와 같다
- **반응 측정:** `ec.measure_reaction(hist, 발표일, "")` — timing 을 비워 D · D+1 중 거래량이 큰 세션을 반응일로 잡는다(`ec.resolve_reaction_index`). Phase 0 A-TIME ❌(`includeReportTimes` 의 2021 이전 채움률 AAPL 26% · WMT 51%)에 따른 약정된 경로다
- **예상 변동폭:** `ec.expected_move(ec.gap_history(hist, past))` — `evaluate_pead` 는 이 중 `median_pct` 만 쓴다

**동결 상수** — 러너는 시작할 때 아래 값을 `earnings_core` 에서 읽어 대조하고, 하나라도 다르면 **수익률을 계산하기 전에** 중단한다. 이 약정은 "지금의 라벨"을 검증하기 때문이다.

| 상수 | 값 | 쓰이는 곳 |
|---|---|---|
| `PEAD_VOLUME_MIN` | 2.0 | 거래량 배수 문턱 |
| `PEAD_SURPRISE_RATIO` | 0.8 | 갭 ≥ 예상 중앙값 × 0.8 → 유의미한 반응 |
| `VOLUME_BASELINE_BARS` | 20 | 거래량 배수 기준 구간(봉) |
| `MIN_SAMPLE` | 4 | 예상 변동폭 최소 표본 |
| `GAP_QUARTERS` | 8 | 예상 변동폭 표본 분기 수 |

**판정 대상 아님:** `up_faded` · `down_recovered`(`관망` — 관찰 문구) · `muted` · `na`.

### 3. 유니버스

| 티어 | 정의 |
|---|---|
| Tier 1 | 러너 실행 당일 Watchlist ∪ Portfolios 티커 — `diag_pead_issuance_probe._load_universe()` 규칙 그대로 |
| Tier 2 | `Earnings_Universe` − Tier 1 에서 **seed 20260911 · 60종목** 무작위 표본 — A4(`diag_pead_date_validity`)와 같은 함수 · 같은 seed |

- 첫 판정 실행은 두 티커 목록을 결과에 그대로 기록한다. 결함 재실행(§8)은 **그 목록**을 쓴다
- 두 티어 모두 **현재 종목**이다 — 생존편향은 §10

### 4. 사건 구성

순서대로 적용한다.

1. **발표일** — `earnings?symbol=X&limit=1000` 에서 `epsActual` 이 있는 행, 같은 날짜 중복 제거(`_reported` 규칙)
2. **분기말 흔적 제외** — `income-statement?symbol=X&period=quarter&limit=80` 의 직전 분기말과 발표일 차이가 **7일 이하**면 제외한다. 직전 분기말을 찾지 못한 사건도 제외한다. A4 의 `PE_LAG_MAX = 7` 과 같다. 이 제외는 판정 사건과 워밍업 사건 모두에 적용한다
3. **구간** — 발표일(FMP 날짜) 기준
   - 판정 구간 **[2012-01-01, 2021-10-20)** — 2021-10-20 당일은 판정 밖이다(2026-09-05 프리뷰 백테스트가 그날부터 봤다)
   - 표시 구간 [2021-10-20, 실행일 − 100달력일] — 판정에 쓰지 않는다
4. **반응 측정** — `measure_reaction(...)["ok"]` 여야 한다. 두 사건이 같은 반응 세션에 걸리면 이른 날짜 하나만 남긴다
5. **워밍업** — `past` = 이 사건보다 이르고, 2012-01-01 이후이며, 2번을 통과한 사건 중 최신순 최대 12건(`GAP_QUARTERS + 4`, 라이브 `past_earnings_dates` 상한과 같다). `expected_move(...)["ok"]` 여야 판정한다. ok 가 아닌 사건은 라벨 집합에도 무조건 집합에도 넣지 않는다 → 종목당 첫 4건 안팎이 소모된다
6. **청산 가격** — 청산 봉 `C[i+60]` 과 진입 · 청산 두 날짜의 SPY 종가가 있어야 한다. 없으면 제외하고 사유별 건수를 기록한다

**가격:** 종목과 SPY 모두 `historical-price-eod/full` — 라이브 `fmp_price_history` 와 같은 엔드포인트 · 같은 `close` 열이다. 모든 FMP 호출은 `fmp_http` 를 거친다(A1 래칫).

### 5. 측정

- `i` = 반응 세션의 위치. **D+k = `C[i+k]`** — 반응 세션 뒤 k 번째 세션의 종가다(프리뷰 백테스트 격자와 같은 표기)
- 진입 D+1 · 청산 D+60 → 종목 수익 `r = C[i+60] / C[i+1] − 1`
- 같은 두 날짜의 SPY 수익 `r_SPY` → **초과수익 `x = r − r_SPY`** (%p)
- **무조건 집합 U** = 티어별 판정 사건 전부(라벨 무관, `muted` 포함). **라벨 집합 L ⊂ U**
- 평균 스프레드 = mean(L) − mean(U)
- 중앙값 스프레드 = median(L) − median(U)
- 연도 스프레드(y) = mean(L_y) − mean(U_y) · y = 발표일 연도 · 2012 ~ 2021 의 10개 해
- 판정 구간에서는 **D+60 외의 지평을 계산하지 않는다**

### 6. 판정 기준

라벨마다, 티어마다 네 조건을 모두 본다.

| | `up_continue` | `down_break` |
|---|---|---|
| **P1** 표본 | n(L) ≥ 100 | n(L) ≥ 100 |
| **P2** 평균 스프레드 | **≥ +1.5%p** | **≤ −1.0%p** |
| **P3** 중앙값 스프레드 | > 0 | < 0 |
| **P4** 연도 일관성 | L_y ≥ 10건이고 연도 스프레드 > 0 인 해가 **6개 이상** | L_y ≥ 10건이고 연도 스프레드 < 0 인 해가 **6개 이상** |

- **라벨 통과 = Tier 1 과 Tier 2 에서 P1~P4 가 모두 ✅ (8칸 전부).** 하나라도 ❌ 면 미달이다
- **표본 부족(P1 ❌)도 미달이다.** "판정 보류"는 없다 — 입증 책임은 라벨에 있다
- 두 라벨은 서로 독립으로 판정한다
- 문턱이 비대칭인 이유: 생존편향이 `up_continue` 는 부풀리고 `down_break` 는 약하게 만든다(§10). 그래서 `up_continue` 에 여유를 더 둔다

### 7. 결과에 따른 조치

| 판정 | 조치 |
|---|---|
| `up_continue` 미달 | 문구 `🟢 상승 지속 후보` → **`🟢 강한 상승 반응`** · `is_candidate` 를 더는 True 로 두지 않는다 |
| `down_break` 미달 | 문구 `🔴 하락 이탈 — 근거 훼손` → **`🔴 강한 하락 반응`** · `is_damage` 를 더는 True 로 두지 않는다 |
| 통과 | 문구를 그대로 둔다. "검증됨" 같은 말을 붙이지 않는다. 매수 신호 · 비중 · 진입 게이트에 아무 영향도 주지 않는다 |

- 코드 값(`up_continue` · `down_break`)은 바꾸지 않는다. `Earnings_Events.PEAD_Verdict` 에 저장된 과거 행이 깨지기 때문이다. 앱 표는 코드 → `ec.PEAD_LABELS` 로 문구를 그리므로, 과거 행도 새 문구로 다시 보인다
- 미달 조치는 판정을 §11 에 기록한 뒤 **다음 커밋에서 바로** 적용한다. `earnings_core.py` 가 바뀌므로 Streamlit 재부팅이 필요하다

### 8. 1회 전용 · 금지 사항

- **수익률을 한 번이라도 계산한 실행은 판정 실행이다.** 판정 실행은 1회다
- 결과를 본 뒤 금지하는 것: 문턱 · 지평 · 구간 · 유니버스 · seed · 사건 규칙 · 라벨 변형을 바꾸는 것, 한 티어만 채택하는 것, 특정 연도나 종목을 빼고 다시 보는 것
- **결함 재실행**은 네 조건을 모두 갖출 때만 한다.
  - (a) 러너가 이 문서와 다르게 구현됐다는 **구체적 결함**이 있다(룩어헤드, 날짜 정렬 오류 등)
  - (b) 재실행 **전에** §11 에 결함과 수정 내용을 적는다
  - (c) 첫 실행이 기록한 티커 목록을 쓴다
  - (d) §6 문턱은 그대로다
  - 결과가 마음에 들지 않는 것은 결함이 아니다
- **무결성 중단** — 러너는 아래를 **수익률 계산 전에** 전부 검사한다. 하나라도 걸리면 수익률을 계산하지 않고 끝낸다. 이 경우는 판정 실행으로 치지 않는다
  - §2 동결 상수 불일치
  - 수신한 SPY 가격의 첫 봉이 2011-12-01 보다 늦다
  - 티어별로 가격 · 실적 · 분기 재무제표 조회에 실패한 종목이 5% 를 넘는다(플랜제한 402 포함)
- **러너 검증 요구** — 실행 전에 합성 세계로 P1~P4 각 조건이 판정을 실제로 뒤집는지(죽은 게이트 없음) 확인하고, 뮤테이션 검증을 거친다
- 판정 실행 뒤 2012 ~ 2021 사건은 PEAD 류 질문에 대해 **본 데이터**가 된다. 이후 다른 PEAD 변형은 이 구간을 미관찰 구간으로 주장할 수 없다
- 표시 구간은 판정에 쓰지 않는다. 표시 구간 결과가 판정과 반대로 나와도 판정은 바뀌지 않는다

### 9. 문턱 근거 — 합성 데이터 검정력

실제 라벨 수익은 한 줄도 보지 않고 **합성 데이터로만** 정했다.

**가정**
- 60세션 초과수익 표준편차 12% · 꼬리는 t(4)
- 연도 × 라벨 충격 표준편차 1%p · 두 티어가 공유(같은 시기라서)
- 라벨 비율 약 11% → 워밍업 뒤 Tier 1 약 280건 · Tier 2 약 195건 (A4 판정 사건 2,818 · 1,991 에서 종목당 워밍업 약 4건 차감)
- 생존편향: `up_continue` 쪽 +0.5%p · `down_break` 쪽 +0.3%p(0 쪽으로)

**§6 규칙 전체(P1~P4 × 두 티어)의 통과 확률**

| 진짜 효과 | `up_continue` 통과 | 진짜 효과 | `down_break` 통과 |
|---|---|---|---|
| 0 | 1% | 0 | 0% |
| +1.0%p | 20% | −1.0%p | 10% |
| +2.0%p | 67% | −2.0%p | 55% |
| +2.5%p | 86% | −2.5%p | 77% |
| +3.0%p | 95% | −3.0%p | 91% |

- 효과가 없으면 거의 통과하지 못한다. 대신 ±1%p 정도의 작은 진짜 효과도 대부분 미달한다
- 이쪽으로 기울인 이유는 비용이 비대칭이기 때문이다. 거짓 통과는 과신하는 라벨을 남기고, 거짓 미달은 문구 두 줄을 바꾼다
- 라벨 비율 11% 는 추정이다. 실제 표본이 추정의 60% 면 `up_continue` +2.5%p 통과율은 56%, `down_break` −2.5%p 는 40% 로 떨어진다. 효과 0 에서는 여전히 1% 이하다
- `down_break` 는 문턱 절대값이 더 작은데도 통과가 더 어렵다. 생존편향이 반대 방향으로 작용하기 때문이며, 의도한 결과다

### 10. 알려진 한계

- **생존편향** — 두 티어 모두 현재 종목이다. 상장폐지된 폭락주가 없어 `down_break` 의 하방 드리프트는 작게, `up_continue` 는 좋게 나온다. Tier 1 은 지금 보유 · 관심 종목이라 사후 선택 편향도 있다. 스프레드(같은 유니버스 대비)와 두 티어 AND 결합, 비대칭 문턱이 일부만 흡수한다
- **배당 미조정 종가** — `full` 의 `close` 는 분할 조정 · 배당 미조정이다. 초과수익의 절대 수준에 배당 차이가 섞이지만 스프레드에서는 대부분 상쇄된다
- **반응일 추론** — 러너는 timing 을 비워 거래량으로 반응일을 잡는다. 라이브는 캘린더에 timing 이 있으면 그것을 쓴다. 두 경로가 다른 날을 고르는 사건에서는 라이브 라벨과 어긋날 수 있다
- **워밍업 직후** — 예상 변동폭 표본이 4~7개인 사건이 섞인다. 라이브 신규 종목과 같은 코드 경로다
- Tier 2 는 60종목 표본이다. 2021년은 10-20 까지만 있는 부분 연도다
- 판정 사건의 D+60 청산은 2022년 초까지 걸친다. 그 시기 가격은 이미 봤지만, 이 사건들의 라벨별 수익은 아무도 계산하지 않았다
- 거래비용 · 세금은 무시한다. 라벨 문구의 예측 의미만 묻기 때문이다

### 11. 개정 이력

판정 결과는 이 표에 **한 행으로** 기록한다. §1~§10 은 고치지 않는다 — 약정문이 결과 전 모습으로 남아야 결과 행과의 시각 대조가 재협상 방지 장치로 작동한다.

| 날짜 | 내용 | 근거 |
|---|---|---|
| 2026-09-11 | 최초 박제. 질문 · 동결 대상 · 유니버스 · 사건 규칙 · 측정 · 문턱 P1~P4 · 조치 문구 · 1회 전용 · 무결성 중단 | Phase 0 `diag_pead_issuance_probe`(193콜 · 402 0건): A-LIMIT 적용 · A-TIME ❌ → 거래량 추론 경로. A-DEPTH 는 날짜의 **존재**만 쟀다는 설계 결함이 드러났다 — 1985년 행은 분기말 날짜였다(MSFT 1985-09-30 행이 1986-03 상장보다 앞선다). → A4 `diag_pead_date_validity`(466콜 · 오류 0): 판정 규칙을 실행 전 docstring 에 박고 돌렸다. 시작 연도 Tier 1 2009 · Tier 2 2012 → 늦은 쪽 **2012**. 판정 사건(분기말 흔적 개별 제외 뒤 · 워밍업 전) Tier 1 2,818 · Tier 2 1,991. 라벨별 분포와 수익률은 **한 번도 계산하지 않았다** |
| 2026-09-11 | **무결성 중단 1건 — 판정 실행 아님.** 러너 결함 1건을 재실행 **전에** 기록한다(§8). 첫 시도(23:30 ET · 수정 전 러너 861줄)는 STEP 1 에서 멈췄다 — Tier 1 조회 실패율 16.0%(17/106) > 5%. **수익률은 한 줄도 계산되지 않았고 결과 탭은 비어 있다.** 원인은 데이터가 아니라 분류 오류다: §8 은 "조회에 **실패한** 종목"을 세는데, 러너가 ETF(COPX·DRAM·FINX·GDX·GNOM·IBIT·JEPQ·OIH·QQQM·SCHD·SKYY·SOXX·UFO·USO·VOO 외 2)처럼 애초에 실적이 없는 종목까지 실패로 셌다. 수정: `profile` 의 `isEtf`/`isFund` 로 **펀드로 확인된 종목만** 제외하고(실패율 분모에서도 제외), 펀드가 아닌데 실적 0행인 종목은 그대로 실패로 센다 — 게이트의 이빨을 남기기 위해서다. 진리표 9종을 셀프테스트 T6 에 고정. 함께 상단 상태판의 '러너 ❌ 미구현' 줄을 현재 사실로 고쳤다 | **§1~§10 은 한 글자도 고치지 않았다** — 문턱 · 구간 · 진입/청산 · 유니버스 · seed · 워밍업 · 분기말 규칙 · 스프레드 정의 · 라벨 문구 전부 불변이고, 바뀐 것은 무결성 카운터의 분류뿐이다. ETF 는 수정 전에도 사건이 0건이었고 지금도 0건이라 **판정 표본은 달라지지 않는다.** 첫 시도 기준값(재실행 대조용): Tier 1 106종목 · Tier 2 60종목 · SPY 5,000봉 2006-10-25 ~ 2026-09-11 · Tier 1 사건 4,071건(판정+표시) · 실패 17종목은 전부 펀드였다 |
| 2026-09-11 | **판정 실행 2026-09-11 20:32 ET — 두 라벨 모두 미달.** 약정 커밋 `17929f9` · FMP 516콜 전부 성공(재시도 0) · Tier 1 실적 89종목(펀드 17 제외) · Tier 2 60종목 · 조회 실패 0 · 판정 사건 T1 2,498 · T2 1,777. **`up_continue`** 평균Δ T1 +0.57%p · T2 +0.68%p (문턱 +1.5) → **P2 ❌** (P1✅ P3✅ P4 6/9✅). **`down_break`** 평균Δ T1 **+0.11%p** · T2 **+0.80%p** (문턱 −1.0 — 부호부터 반대) → **P2 ❌ · P4 ❌**(5/9 · 4/9). 네 칸 전부 P2 에서 떨어졌다 — 경계선 판정이 아니다. §7 대로 문구를 교체한다: `🟢 상승 지속 후보` → `🟢 강한 상승 반응` · `🔴 하락 이탈 — 근거 훼손` → `🔴 강한 하락 반응` · `is_candidate`·`is_damage` 는 더 이상 True 가 되지 않는다. 코드 값과 `Earnings_Events.PEAD_Verdict` 과거 행은 불변 | 기록 전 독립 검산: 라벨 분포 합 = 판정 사건 수(2,498 · 1,777) 일치 · 네 칸의 평균Δ·중앙Δ 재계산 일치 · 사건 수가 A4(2026-09-11) 대비 두 티어 모두 약 +1% 로 같은 방향. **관찰 2건 — 판정을 바꾸지 않는다.** ① **2012 년은 구조적으로 비었다.** 워밍업 표본을 2012-01-01 이후로 제한했으니 종목마다 첫 4건이 2012 년에 소모된다. 그래서 P4 유효 연도가 10 이 아니라 **9** 였고 문턱 6 은 6/9 로 적용됐다 — 약정이 의도한 6/10 보다 미세하게 빡빡하다. 네 칸 모두 P2 에서 먼저 떨어졌으므로 판정에는 영향이 없다. ② **이미 본 구간(표시, 2021-10-20 이후)은 Tier 1 에서 방향이 맞았다** — up +2.00%p · down −2.15%p 로 판정 구간과 반대다. §8 대로 표시 구간은 판정에 쓰지 않는다. 이 대비가 사전 약정을 한 이유 그 자체다. 부수: T2 연도 부호 집계 두 해(up 2015 · down 2017)는 출력 반올림으로 0.0 으로 보이는 값에 걸려 있다 — P2 가 이미 미달이라 무관하다 |
