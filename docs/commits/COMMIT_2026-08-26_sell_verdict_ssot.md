fix(diag): diag_sell_verdict 복제본이 SSOT 와 갈라진 것 수정 + 격자 대조 신설

## 발단

`regime_core` 52주 창 수정 배포 후 `diag_sell_verdict` 재실행에서 빨간불:

```
❌ SSOT 대조 실패 — 진단 배점이 regime_core 와 다르다:
   PNC:  진단 🟡 줄이기 vs regime_core ✅ 보유 (HOLD)
   CSCO: 진단 🟡 줄이기 vs regime_core ✅ 보유 (HOLD)
```

## 우리 수정과 무관하다 — 실증 확인

구/신 `regime_core` 에 동일 입력을 넣어 대조했다:

| 종목 | 구 regime_core | 신 regime_core | 동일 |
|---|---|---|---|
| PNC | ✅ 보유 (HOLD) | ✅ 보유 (HOLD) | ✅ |
| CSCO | ✅ 보유 (HOLD) | ✅ 보유 (HOLD) | ✅ |

`W52_BARS` 는 `classify_regime` 226행 한 곳에서만 쓰이고
`integrated_sell_verdict` 경로에 닿지 않는다. **기존 잠복 결함**이다.

## 진짜 원인 — 채택된 제안이 복제본에 반영되지 않았다

| | 진단 복제본 (`현재` 열) | regime_core (SSOT) |
|---|---|---|
| 1개월 | `om < 0` → +1 | `om <= POSITION_MONTH_DROP_PCT(-5.0)` → +1 |
| 200일선 이탈 | 일괄 4점 | 이격 비례 **선형 램프** 2.0~4.0 |

PNC·CSCO 둘 다 1개월 **−2.8%** — 복제본은 +1(합 2 → 줄이기), SSOT 는 0(합 1 → 보유).

**데이터 의존 잠복 결함이다.** 보유 종목의 1개월 수익률이 −5%~0% 구간에
들어와야만 터진다. 그 사이 [3] 블록은 계속 초록불이었다.

### 더 큰 문제 — 시나리오 열이 프로덕션 대 프로덕션이었다

`D_MONTH_THRESHOLD = -5.0` 인데 SSOT 도 `-5.0` 이다. 즉 **`3D` 는 "제안"이 아니라
이미 프로덕션**이었다. 표에서 `3D` 열이 SSOT 와 일치하고 `현재` 열이 틀렸다 —
**두 열의 의미가 뒤바뀌어 있었다.**

`3E` 도 마찬가지다. `gap_ma200_pct` 를 따라가 보면:

| 호출부 | `gap_ma200_pct` | 결과 |
|---|---|---|
| `app.py:10834` | ✅ | **비례 2.0~4.0 (live)** |
| `regime_core.py:2407` | ✅ | 비례 (live) |
| `diag_sell_verdict.py:458` | ❌ | 일괄 4점 폴백 |
| `run_signal_backtest.py:500` | ❌ | 일괄 4점 폴백 |

게다가 이 파일의 `E_LADDER = ((-3,2),(-8,3))` 는 **계단식**인데 SSOT 는
**선형 램프**(`2.0 + 2.0 × min(1, |gap|/8.0)`)다. 채택된 것과 형태가 다르다.

**파급**: `3D`/`3E` vs `현재` 비교로 내린 과거 결론이 있다면, 그건 설계 선택지를
잰 게 아니라 **복제본의 버그를 잰 것**이다.

## 변경 파일

| 파일 | 위치 | 줄 수 | 이전 |
|---|---|---|---|
| `diag_sell_verdict.py` | `automation/` | **659** | 582 |

단일 파일. 락스텝 없음. 원시 `requests.get` 1곳 유지 →
`diag_fmp_ssot._RAW_GET_BASELINE` 무변경.

## 수정 내용

**1. 임계값 복제 제거 — 전부 `rc` 에서 읽는다**
```python
if pd.notna(om) and om <= rc.POSITION_MONTH_DROP_PCT:
c["ma200"] = rc.MA200_GAP_BASE_SCORE + (4.0 - rc.MA200_GAP_BASE_SCORE) * min(
    1.0, _g / rc.MA200_GAP_FULL_PCT)
```
상수를 이 파일에 복제하면 SSOT 가 바뀔 때 조용히 갈라진다 — 실제로 갈라졌다.

**2. SSOT 대조에 `gap_ma200_pct` 전달**
안 넘기면 SSOT 가 일괄 4점 폴백으로 가는데 프로덕션은 넘긴다. 즉 **프로덕션이
아닌 설정과 대조**하게 된다.

**3. `3D`·`3E` 열 제거 + 상수 삭제 + 묘비명**
`[3]` 이 `현재` / `문턱5` 2열로 줄었다. `SELL_TH_ALT = 5` 는 아직 미채택이라 남겼다.
헤더에 "다시 추가하지 말 것" 경위를 기록했다.

**재정의하지 않은 이유**: 새 임계값(−8% 등)을 넣는 것은 **새 질문을 발명하는
것**이다. 사전 기준도, 지금 값이 나쁘다는 근거도 없다. §8 이 금지한 "돌릴 손잡이
찾기"가 된다.

**4. `[S]` SSOT 격자 대조 신설 — 57,600 조합 전수**
실보유 종목만으로 대조하면 그날 우연히 판별 구간에 들어야만 잡힌다. 경계값을
직접 만들어 전수로 돌린다:

`above_ma200`(2) × `gap200`(9) × `1개월`(8) × `MACD`(4) × `RSI`(4) ×
`고점대비`(5) × `낙폭`(5) = **57,600**

FMP·시트 접근 **이전에** 실행한다. 복제본이 갈라져 있으면 아래 블록 숫자가 전부
무의미하므로, 키가 없어 중단되는 경우에도 이 결과만은 남아야 한다.

**5. 종료코드** — SSOT 가 갈라진 채 초록불을 내면 다음에도 못 잡는다.
대조 실패 시 `return 1`.

## 검증

| 항목 | 결과 |
|---|---|
| `py_compile` · `check_py311` | OK · 3.11 호환 |
| 격자 대조 (신 regime_core) | **57,600/57,600 일치** |
| 격자 대조 (구 regime_core) | **57,600/57,600 일치** ← 우리 수정과 무관함 재확인 |
| 잔존 참조 | `use_3d`/`use_3e` 0건 · `D_MONTH_THRESHOLD`/`E_LADDER` 묘비명 주석 1줄만 |
| 뮤테이션 역검증 | **6/6 검출** + 조합 뮤테이션 1건 |

검증은 파일을 import 하지 않고 **AST 로 함수·상수를 추출**해 돌렸다
(§6-7 — 이 파일은 gspread·requests 를 최상위에서 임포트한다).

| # | 뮤테이션 | 결과 |
|---|---|---|
| R1 | 1개월 임계값을 옛 코드로 (`om < 0`) | ❌ 3,420건 |
| R2 | 200일선 램프 → 일괄 4점 | ❌ 4,662건 |
| R3 | 램프 상수 복제 (2.0 하드코딩) | ❌ 1,962건 |
| R4 | MACD BELOW_SIGNAL 배점 누락 | ❌ 4,464건 |
| R5 | 과열 RSI 경계 70 → 65 | ❌ 1,434건 |
| R6 | 트레일링 3점 → 2점 | ❌ 3,924건 |
| R7 | 격자에서 `-5.0` 경계값 제거 | ✅ 통과 (단독으론 무해) |
| **R8** | **R7 + R1 조합** | ✅ **통과 — 경계값이 판별력의 원천** |

**R8 이 핵심이다.** 격자에서 경계값(`-5.1`/`-5.0`/`-4.9`/`-2.8`)을 빼면 R1 의
드리프트가 **초록불로 통과한다.** 격자는 조합 수가 아니라 **경계를 덮는가**로
값이 정해진다. 새 임계값이 SSOT 에 생기면 `_G_*` 에 그 경계를 추가해야 한다.

## 별건으로 분리 — `run_signal_backtest.py:500`

```python
def _pos_label_at(...):
    """... integrated_sell_verdict (SSOT 그대로)."""   # ← 이 주석이 틀렸다
```

`gap_ma200_pct` 를 안 넘겨 **백테스트는 200일선 이탈에 일괄 4점(🔴 청산),
프로덕션은 이격 0% 부근에서 2.0점(🟡 줄이기)** 이다. 200일선 바로 아래 종목에서
판정이 갈린다. `feats` 에 `ma200` 이 이미 있으므로(477행) 데이터는 있다.

**영향 범위**: `_pos_label_at` 은 매도 판정이므로 `Signal_Backtest`(entry/wait/…)
표가 아니라 **`Signal_Backtest_RT`(왕복) 시트**에 닿는다.

**이번에 묶지 않았다.** 백테스트 수정은 RT 결과를 바꾸므로 별도 기준선과 별도
결정이 필요하고, 같은 세션에 `regime_core` 와 함께 바꾸면 무엇이 무엇을 바꿨는지
분리가 안 된다.

## 남은 한계

- 격자는 **라벨**만 대조한다. 점수 자체나 `reason` 문자열은 안 본다
- `_G_*` 는 현재 SSOT 의 경계에 맞춰져 있다. **SSOT 에 새 임계값이 생기면 격자에
  경계를 추가해야 한다** — 안 하면 R8 상태가 된다
- `trailing_stop_pct` 는 기본값 하나만 돌린다. 계좌별 트랜치 설정이 다른 값을
  쓰면 그 조합은 미검증
- `[4]` 블록은 이제 SSOT 램프 기준으로 표시만 바꿨다. 분포 자체의 용도(경계 결정)는
  3E 채택으로 이미 끝났다
