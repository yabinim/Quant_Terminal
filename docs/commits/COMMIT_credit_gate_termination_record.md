docs(satellite): §3 크레딧 게이트 보강 시도 종료 기록 + 드리프트 가드 L 그룹

## 왜

크레딧 게이트(Track C 후보 6번)는 2026-09-13 실행으로 **조기경보 없음 → 종료**로
판정됐으나, 그 판정이 대화와 프로브 docstring 에만 있었다. 원칙 문서에 없으면
미래 세션이 "SPY200 에 크레딧 축을 붙이자"를 다시 제안한다 — 실제로 이 후보는
한 번 C 트랙 목록에서 조용히 빠졌고, 종료 직후에도 "2차 실행 대기"로 잘못
읽힌 적이 있다. C-1(β중립 12-0) 종료 때와 같은 처리를 한다.

## 변경 — SATELLITE_MANDATE.md (205 → 213줄, GitHub 표시)

### §3 각주 추가 — "보강 시도 1 — 크레딧 게이트 (2026-09-13 실행 · 조기경보 없음 → 종료)"

- 판정 — 커버된 5개 사건 중 리드 1개(20%), 문턱 과반 미달. 사건별 리드/래그
  (2015-16 −14 · 2018 Q4 −39 · 2020 −241 · 2022 **+59** · 2025 −24일).
  사건 1(2008)은 HYG 상장일(2007-04-11) 때문에 커버 부족으로 제외
- 벤더 2회 사용 이력 — 1차 FRED `BAMLH0A0HYM2` 의 ❌ 는 **경제적 결과가 아니라
  데이터 단절**(2026-04 ICE 라이선스 변경, 롤링 3년 창)이었으므로 판정으로 읽지
  않았다. 사건 정의·게이트 형태·문턱을 고정한 채 수신 함수만 FMP HYG/LQD 로 교체
- 참고(판정 미사용) — 2020 의 −241일은 `ma_crossdowns` 의 "새 교차" 정의에서
  오는 방법론적 유의점. 이 사건을 빼도 1/4(25%)로 결론 불변
- 종료 범위 — 닫힌 질문은 "HYG/LQD 200일선 하향교차가 SPY 200일선 이탈보다
  **먼저** 신호를 내는가"뿐. 크레딧 지표 자체에 대한 판정이 아니다.
  **세 번째 벤더도 다른 트리거 정의도 이 종료의 재시험이다** — 재시험 금지
- **§3 필터는 그대로 남는다** — 축을 더하는 데 실패한 것이지 기존 축이 흔들린
  게 아니다

### §7 개정 이력 1행 추가 (2026-09-13)

## 변경 — diag_satellite_mandate.py (872 → 979줄, GitHub 표시)

새 **L 그룹 6항목** + **H 양성 대조 3항목**. 헤더 그룹 목록에 L 추가.

| 검사 | 무엇을 막나 |
|---|---|
| L0 | 프로브 소스 부재 — 없으면 L2·L5 가 거짓 초록이 된다(존재 게이트) |
| L1 | 각주가 §3 **안에서** 종료·종료범위·'재시험 금지'를 유지 |
| L2 | 각주의 문턱 = 프로브 `C_LEAD_PASS_FRAC` (한쪽만 고치면 걸린다) |
| L3 | **각주 본문이** 프로브 파일·데이터 경로를 지목 |
| L4 | §3 필터 본문 불변 |
| L5 | 프로브가 사건 정의를 `dmr` 에서 받아 쓴다(자체 리터럴 금지) |
| H20 | 종료 범위 문장만 지운 md 는 L1 을 통과하지 못한다 |
| H21 | 각주를 §4 뒤/§3 앞으로 옮긴 md 는 L1 을 통과하지 못한다 |
| H22 | 문턱을 바꾼 프로브는 L2 를 통과하지 못한다 |

**K12 와 충돌하지 않게 한 것** — §3 각주의 종료 범위 앵커를
"세 번째 벤더도 다른 트리거 정의도"로 따로 뒀다. §2 각주의
"이 종료의 재시험으로 본다"를 그대로 쓰면 H18 의 치환 앵커가 두 곳을 가리켜
그 대조가 엉뚱한 곳을 시험하게 된다(H18 주석이 경고하는 함정).

**설계 중 잡은 결함 1건** — L3 의 첫 판은 `"diag_credit_gate_probe.py" in MD`
였는데, 같은 이름이 §7 개정 이력 행에도 있어 **각주에서 지워도 통과**했다
(뮤테이션 M5 생존으로 발견). 각주 본문만 잘라 보도록 고쳤다.

## 검증

- `py_compile` 통과 · `check_py311` 1/1 · `pyflakes` 0경고(기준선도 0)
- `diag_satellite_mandate` **145/145 통과 · 실패 0** (이전 136 → +9)
- 뮤테이션 **10 killed / 0 survived** — L0~L5 각각 최소 1건으로 개별 사살 확인
  (md 문턱·프로브 문턱 양방향, 사건 임계 자체 리터럴화, 필터 본문 변조,
  각주 내 '재시험 금지'만 삭제, 프로브 파일 삭제 포함)

## 락스텝 배포 순서

1. `SATELLITE_MANDATE.md` → 루트 (213줄)
2. `diag_satellite_mandate.py` → `automation/` (979줄)

**역검증 한 방향만 성립한다(정직하게 적는다).** 구 md + 신 가드 → L1·L2·L3
3건 실패로 즉시 걸린다. 반대(신 md + 구 가드)는 **조용히 통과**한다 — 구
가드에 L 그룹이 없어서다. 가드를 새로 추가하는 변경의 구조적 성질이며,
그래서 두 파일을 한 커밋으로 올린다.

## Streamlit 리부팅

불필요 — `app.py`·`fmp_extras.py` 미변경.

## 한계 · 후속

- 롤백 — 두 파일 되돌리면 원상복구. 데이터 손실 없음

---

# 별도 커밋 — chore(freshness): 크레딧 게이트 종료 기록 · 순발행 약정 마커 등록

## 변경 — check_freshness.py (342 → 349줄, GitHub 표시)

1. `SATELLITE_MANDATE.md` 마커에 **`"보강 시도 1 — 크레딧 게이트"`** 추가 (3 → 4개).
   앞의 3개는 β중립 블록에만 있어 **크레딧 게이트 종료 기록 전/후 사본을 가르지
   못한다.** 낡은 사본으로 §3 을 고치면 방금 쓴 종료 기록이 날아간다.
2. **`ISSUANCE_PRECOMMIT.md` 신규 등록** — 지금까지 지문 표에 아예 없어서 낡아도
   아무 경고가 안 났다. 마커 `ln(2)`(M&A 제외 문턱) · `미검증`(§7 허용 표시의
   캡션 조건) · `635`(3%p 를 80% 검정력으로 잡는 데 필요한 분기 수 ≈159년) ·
   **`그리고 종료 판정`**(§9 헤더 — 이것만 종료 기록과 함께 들어왔다).
   md 가 3/4 이면 종료 판정 기록 **전** 사본이다.

## 검증

- `py_compile` · `check_py311` 1/1 · `pyflakes` 0경고
- 지문 표 신규 2행 — `SATELLITE_MANDATE.md` 214 **4/4** · `ISSUANCE_PRECOMMIT.md`
  238 **4/4** · 정합성 ✅
- **마커 판별력 실측(뮤테이션)** — 각주 없는 구 md 투입 → `205 3/4 ⚠ 누락:
  보강 시도 1 — 크레딧 게이트`. 종료 기록 전 상태를 흉내낸 ISSUANCE 사본 투입
  → `238 3/4 ⚠ 누락: 그리고 종료 판정`. 둘 다 조용히 통과하지 않는다

## 배포

- `check_freshness.py` → 루트 (349줄). **단독 커밋** — 락스텝 파트너 없음,
  Streamlit 리부팅 불필요
- ⚠️ 순서 — 위 §3 각주 커밋을 **먼저** 올린다. 이 파일만 먼저 올리면
  `SATELLITE_MANDATE.md` 가 3/4 로 뜬다(오탐이 아니라 정확한 경고다)
- 롤백 — `MARKERS` 항목 2건만 되돌리면 원상복구. 데이터 손실 없음
