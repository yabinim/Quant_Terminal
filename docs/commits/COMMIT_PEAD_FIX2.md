fix(pead): 2차 시도 크래시 수정(SPY 호출부) + 셀프테스트가 main() 을 통째로 돌게

2026-09-11 2차 시도는 `AttributeError: 'tuple' object has no attribute 'empty'`
로 죽었다. **판정 실행이 아니다** — 수집 도중 크래시라 수익률 계산도, 시트 쓰기도
없었다. 결과 탭은 여전히 비어 있고 1회 전용 가드는 살아 있다.

내 실수다. 1차 수정에서 `price_frame` 을 `(df, kind)` 튜플 반환으로 바꾸면서
`main()` 의 SPY 호출부 한 곳을 낡은 채로 두었다.

---

## 진짜 원인은 한 줄이 아니다

셀프테스트가 순수 함수(`build_events` · `attach_returns` · `judge_tier` ·
`classify_status`)만 때리고 **`main()` 을 한 번도 돌리지 않았다.** 그래서
"수집 → 무결성 → 판정 → 기록"을 잇는 배선이 끊겨도 초록불이 났다.
`py_compile` 과 `check_py311` 은 문법만 본다 — 이 부류를 못 잡는다.

한 줄만 고치면 다음에 같은 자리에서 또 죽는다. 배선을 검사 대상에 넣었다.

## 고친 것 — `automation/diag_pead_label_verify.py` (1087줄)

### 1. SPY 호출부 (버그)
```python
spy_df, spy_kind = price_frame("SPY")
if spy_df.empty or spy_kind != "ok":
```
변경된 세 함수(`price_frame` · `earnings_rows` · `quarter_ends`)의 호출부를
전수 확인했다 — 남은 곳은 이 하나였고, 지금은 `fetch_ticker` 와 여기 둘뿐이다.

### 2. 오프라인 E2E — 신규 T7
네트워크도 시트도 없이 **`main()` 을 통째로** 굴린다. `_json` · `_open_sheet` ·
`fh.fmp_key` · `V._universe` 를 합성 데이터와 가짜 시트로 갈아끼우고 실행한 뒤
원상복구한다. FMP 응답 모양(가격·실적·분기재무·프로필)을 그대로 흉내 낸다.

| 검사 | 내용 |
|---|---|
| 정상 경로 | 종목 12 + ETF 1 → exit 0 · 결과 탭 12행 · 판정 행 4개(티어 2 × 라벨 2) |
| 펀드 처리 | `펀드 제외 1종목` · `조회 실패 0종목` · 메타 행에 티커 기록 |
| 중단 경로 | 한 종목을 `plan_limited` 로 → exit 1 · **시트에 아무것도 쓰지 않음** · 실패 종목 로그 |
| 예외 | `main()` 이 터지면 트레이스백 대신 ❌ 로 보고하고 나머지 검사를 계속한다 |

**역검증했다.** 고친 줄을 원래 버그로 되돌리고 셀프테스트를 돌리니 T7 이
`❌ main() 이 예외 없이 끝난다 — AttributeError: 'tuple' object has no attribute 'empty'`
로 잡는다. 워크플로에서 셀프테스트는 판정 **앞 스텝**이므로, 이런 판은 판정을
시작하지도 못한다.

T7 은 합성 데이터로 수익률을 계산한다. 실제 데이터도, 실제 시트도 건드리지 않는다.

## 약정 변경 없음

`PEAD_PRECOMMIT.md` 를 손대지 않는다. 문턱 · 구간 · 유니버스 · seed · 라벨 전부
그대로이고, 이번 수정은 순전히 배선과 테스트다. 러너의 약정 대조 17항목이
실행 때마다 다시 확인한다.

§11 에 행을 추가하지 않는다. 직전 행이 이미 "무결성 중단 · 재실행 예정"을
기록하고 있고, 이번 건은 **수익률에 닿지 못한 크래시**다. 굳이 남기고 싶으면
다음 판정 결과 행의 근거 칸에 한 문장으로 적으면 된다:
`2차 시도(2026-09-11)는 SPY 호출부 크래시로 수집 단계에서 종료 — 수익률 미계산`

## 검증

- 셀프테스트 T0~T7 **전부 통과**. T7 9항목 신규
- 오탐률(T1b) 효과0 up 1% · down 2% · ±2.5%p 67% / 87% — 변동 없음
- `check_py311.py` ✅ · `py_compile` ✅ · 마커 3/3

## 배포 체크리스트

### 1. 배치
| 경로 | 지문표 표기 | GitHub 표시 |
|---|---|---|
| `automation/diag_pead_label_verify.py` (덮어쓰기) | **1088** | 1087 |

- 이 파일 하나다. md · 워크플로 · `check_freshness.py` 무변경
- 공용 모듈 무변경 · 재부팅 불필요

### 2. 재실행
```
Actions → 🔬 판정 — PEAD 라벨 검증 → Run workflow
  confirm          : PEAD-JUDGE
  precommit_commit : <약정 최초 커밋 7자리>
```
- 셀프테스트 스텝(T0~T7)이 먼저 돈다. 여기서 빨간불이면 판정은 시작되지 않는다
- 예상 로그: `Tier 1: 사건 …건 · 실적 종목 89 · 펀드 제외 17종목 · 조회 실패 0종목(0.0%)`
- 약 400~420콜 · 10~20분

### 3. 롤백
- 이 파일만 되돌리면 된다. 데이터 손실 없음

## 한계 · 다음

- T7 은 배선을 검사한다. 수치의 옳고 그름은 T1~T4(합성 세계 · 죽은 게이트 ·
  뮤테이션 6종)가 본다. 두 축이 서로를 대신하지 못한다
- 이번에도 무결성 게이트를 넘지 못하면 그때는 데이터 문제로 보고 종목 목록을
  따로 판정한다. 문턱을 올리는 것은 §8 위반이다
- 완주하면 그 실행이 유일한 판정이다. 그 뒤: §11 결과 행 · md 네 번째 마커 ·
  미달 라벨 문구 교체(재부팅 필요)
