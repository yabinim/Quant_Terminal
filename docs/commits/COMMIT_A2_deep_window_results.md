docs(satellite): 깊은 창 참고 실행 결과 기록 (§2 알려진 약점 · §7) + 절삭 구간 측정 결함 수정

## 배경

커밋 ①(사전 약정 + 러너)을 배포한 뒤 참고 실행을 1회 돌렸다.
- 실행 시각: 2026-09-11 03:54 UTC (ET 09-10)
- 사전 약정에 따라 결과를 쓸 수 있는 곳은 §2 각주의 "알려진 약점"뿐이다. 이 커밋은 그 기록이다.
- **규칙·필터·트리거는 하나도 바꾸지 않았다.**

## 실행 결과 요약

**수신**
- 114콜 전부 성공
- SPY 원종가·배당조정 모두 5,000봉, 2006-10-24 ~ 2026-09-10
- 배당조정 깊이는 이번이 첫 실측이다. 원종가와 같은 롤링 5,000 레코드이고 바닥도 같다.

**평가 구간**
- 2007-10-26 ~ 2026-09-10 (워밍업 253봉)
- 2008년에도 11섹터 전부 챔피언이 있었다. 후보는 30개(현재 56개)였다.

**사건 6개** (SPY 원종가 −12%)

| # | 고점 → 바닥 | 낙폭 | 비고 |
|---|---|---|---|
| 1 | 2007-10-09 → 2009-03-09 | −56.5% | 절삭 |
| 2 | 2015-05-21 → 2016-02-11 | −14.4% | |
| 3 | 2018-09-20 → 2018-12-24 | −20.2% | |
| 4 | 2020-02-19 → 2020-03-23 | −34.1% | |
| 5 | 2022-01-03 → 2022-10-12 | −25.4% | |
| 6 | 2025-02-19 → 2025-04-08 | −19.0% | |

기록: 72행.

**사전 예측 오류**
- 사건을 8개로 예상했지만 6개였다.
- SPY 원종가가 2007년 고점을 2013-03-14에야 회복했다. 그래서 2010·2011 조정이 정의대로 사건 1에 병합됐다.
- 정의는 사후에 바꾸지 않았다.

## 파일별 변경

**`SATELLITE_MANDATE.md`**
- §2에 "깊은 창 참고 실행에서 드러난 알려진 약점" 각주를 추가했다. 세 가지다:
  - 룰 자체로는 하락을 막지 않는다. 방어는 §3 필터가 했다.
  - 필터의 대가는 V자 반등이다.
  - A반·B반 각각의 붕괴 모양.
- 각주 끝에 **"이 사실로 규칙·필터·트리거를 바꾸지 않는다"**를 명시했다.
- §7에 결과 행을 추가했다.

**`automation/diag_momentum_deep_ref.py`** — 측정 결함 1건 수정
- **증상:** 절삭된 사건 1 하락 구간에서 수익률·초과가 6개 설정 전부 n/a였다. MDD는 정상이었다.
- **원인:** 절삭 구간의 시작일은 곧 평가 시작일이다. 이 날은 항상 첫 체결일보다 앞선다. 그래서 곡선에 시작값이 없었다.
- 롤링 바닥 때문에 앞으로의 참고 실행에서도 첫 사건은 계속 절삭된다. 즉 이 결함은 반복된다.
- **수정:**
  - `leg_metrics` 는 곡선이 늦게 시작하면 곡선의 첫 값부터 잰다. 실제 측정 시작일을 반환한다.
  - 호출부는 SPY도 **같은 날부터** 잰다. 기간이 다른 두 수익률의 차이는 초과수익이 아니기 때문이다.
  - 시트에 `Meas_Start` 열을 맨 뒤에 신설했다. 기존 72행은 이 열이 공란이다.
  - 측정 시작일이 밀리면 로그에 `ⓘ 측정 시작 … (첫 체결일)` 을 찍는다.
- 결함은 룰과 무관하다(전 설정 동일 n/a). 그 구간의 질문은 MDD로 이미 답이 나왔으므로 **재실행하지 않는다.**
- 자체검증 17개 항목. 절삭 구간 측정 항목을 신설했다. 옛 "첫 체결 전은 nan" 기대는 새 동작 기대로 교체했다.

**`automation/diag_satellite_mandate.py`** — 115 → 117건
- J8: §2 약점 각주가 §3보다 앞에서 "바꾸지 않는다" 문장을 유지하는지 본다.
- H17: 그 문장만 지우면 J8이 잡는지 확인하는 양성 대조다.
- 이유: 약점 목록은 읽기 좋은 근거라서, 나중에 이 문장만 지우고 "그래서 필터를 바꿨다"로 이어 쓰기 쉽다.

**`check_freshness.py`** — 딥 러너 마커에 `Meas_Start` 를 추가했다. 옛 사본이면 절삭 구간이 다시 n/a가 된다.

## 검증

| 검사 | 결과 |
|---|---|
| py_compile · check_py311 | ✅ (46파일) |
| pyflakes | 델타 0 |
| diag_satellite_mandate | ✅ 117건 (+J8·H17) |
| diag_fmp_ssot | ✅ 70건 (불변) |
| diag_satellite_backtest · diag_momentum_rule_compare `--selftest` | ✅ (불변) |
| diag_momentum_deep_ref `--selftest` | ✅ 17개 항목 |
| check_freshness | ✅ 딥 러너 4/4 |
| 뮤테이션 | **23 killed / 0 survived** (신규 M20~M23 포함, 사망 사유 개별 확인) |

신규 뮤테이션:

| 번호 | 변경 | 잡은 검사 |
|---|---|---|
| M20 | SPY를 구간 시작일부터 잼 | 자체검증 10 (−17.97 ≠ −17.69) |
| M21 | 곡선 첫 값 이동 제거 | 자체검증 3·10 |
| M22 | md "바꾸지 않는다" 삭제 | J8 |
| M23 | 행에서 `Meas_Start` 누락 | 자체검증 (IndexError로 크게 죽음) |

## 배포 순서 (dev 직접 덮어쓰기)

1. `SATELLITE_MANDATE.md` — J8은 이 문장이 있어야 통과하므로 가드보다 먼저 올린다.
2. `automation/diag_satellite_mandate.py`
3. `automation/diag_momentum_deep_ref.py`
4. `check_freshness.py`

Streamlit 리부트 1회 — md가 앱 위성 섹션에 렌더된다.

## 한계 · 후속

- 사건 6개, 사건마다 한 경로뿐이다. T6(후보 풀 편향)는 그대로다. 통계적 주장은 하지 않는다.
- `no_new` 는 §3의 근사다. 실제 §3의 "비중 축소"는 모델에 없다. 반등 비용 수치는 "현금화 후 신규 매수 중단"을 가정한다.
- 2010·2011 조정은 이번 정의로는 재지 않았다. 쪼개려면 새 사전 약정이 필요하다.
- 후보 (미결정): §3 필터 재진입 규칙 검토. 약정대로 별도 작업이며 새 사전 약정이 필요하다.
- 전체 워크플로의 `actions/checkout@v4`·`setup-python@v5` 가 Node 20 사용 중단 공지를 낸다. 현재는 Node 24로 자동 실행돼 무해하다. 일괄 정리 항목으로 둔다.
